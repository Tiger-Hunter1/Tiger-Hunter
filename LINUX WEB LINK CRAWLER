GNU nano 9.0                 6.py
import requests
from bs4 import BeautifulSoup
import os

os.system("clear")
print("=" * 35)
print("[+]CUSTOME LINUX WEB LINK CRAWLER")
print("=" * 35)

target_url = input("Enter Target Website:")

print(f"[+]Fecthing Link From {target_url}...")
print("-" * 40)

headers = {"user_Agent": "Mozila/5.0 (windows NT 10.0; win64;>

try:
    response = requests.get(target_url, headers=headers, time>

    soup = BeautifulSoup(response.text, "html.parser")

    links=soup.find_all("a")

    print(f"[+] Found {len(links)} links kn the page:\n")

    for links in links:
      href = links.get("href")

      if href:
         print(f"[->] Link: {href}")

except requests.exceptions.ConnectionError:
    print("[!] Error: Internet Connection Problem!")
    print("[!] Error: Problem or Invalid URL!")

except Exception as e:
    print(f"[!] An error occurred: {e}")

print("-" * 50)
print("[+] Task Completed!")
